<form method="post" action="{{ path('app_trajet_delete', {'idTrajet': trajet.idTrajet}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trajet.idTrajet) }}">
    <br>
    <button class="btn btn-primary">
  <i class="bi bi-trash"></i> Delete
</button>

</form>
