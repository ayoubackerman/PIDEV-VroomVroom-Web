<form method="post" action="{{ path('app_voiture_urgence_delete', {'idVoiture': voiture_urgence.idVoiture}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ voiture_urgence.idVoiture) }}">
    <button class="btn">Delete</button>
</form>
