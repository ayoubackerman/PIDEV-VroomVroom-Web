<form method="post" action="{{ path('app_urgence_delete', {'idUrgence': urgence.idUrgence}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ urgence.idUrgence) }}">
    <button class="btn">Delete</button>
</form>
