{% extends 'base.html.twig' %}


{% block body %}

<img src="{{asset('images/bg7.jpg')}}" alt="" style="margin: 0;>
	<div class="row" style="display: flex;align-items: center;justify-content: center;margin-top:150px;margin-left:10px;">
		<div class="col-lg-10" style="margin-left:100px; margin-rigth:100px;">
			
      <div class="card">
<br>
<br>
<div class="container text-center border border-warning p-3">
  <h1 style="color: #43aeff; font-family: 'Times New Roman', Times, serif;">
    <i class="fas fa-calendar-plus" style="color: blue;"></i> <span style="color: #43aeff;">Ajouter Reservation</span>
  </h1>
</div>
<br>
<br>
	<style>
  
  .card {
    border: none;
    box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
  }

  .card-header {
    background-color: #f2f2f2;
    font-weight: bold;
  }

  .card-body {
    padding: 1.25rem;
  }

  .card-body p {
    margin-bottom: 0;
    display: flex;
    align-items: center;
  }

  .card-body p i {
    margin-right: 0.5em;
  }

  .card-body p .price {
    color: green;
  }

  .card-body p .num-places {
    color: blue;
  }

  .card-body p .payment {
    color: orange;
  }

  .card-body a {
    color: red;
    font-family: Helvetica, sans-serif;
    font-size: 1.5em;
    text-decoration: none;
  }

  .card-body a:hover {
    text-decoration: underline;
  }
  
  /* Add styles for the car icon and bold cities */
  .card-header .fa-car {
    margin-right: 0.5em;
    color: #007bff;
  }
  
  .card-header .city {
    font-weight: bold;
  }
  
</style>
<input type="search" id="myInput" class="form-control pl-6" placeholder="Search "/><script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.3/jquery.min.js"></script><script>$(document).ready(function () {$("#myInput").on("keyup", function () {var value = $(this).val().toLowerCase();$("#myTable " ).filter(function () {$(this).toggle($(this).text().toLowerCase().indexOf(value) > -1)});})});</script>


{% for trajet in trajets %}

  <div class="card mb-3" id="myTable">
    <div class="card-header"><i class="fas fa-car"></i><span class="city">{{ trajet.villeDepart }}</span> - <span class="city">{{ trajet.villeDarrive }}</span></div>
    <div class="card-body">
      <p><i class="fas fa-money-bill-wave"></i> Prix: <span class="price">{{ trajet.prix }}</span></p>
      <br>
      <br>
      <p><i class="fas fa-user-friends"></i> Nombre de places: <span class="num-places">{{ trajet.nbrPlace }}</span></p>
      <p><i class="fas fa-credit-card"></i> Mode de paiement: <span class="payment">{{ trajet.modePaiement }}</span></p>
      <p><i class="far fa-calendar-alt"></i> Date: {{ trajet.date  }}</p>
      <br> <br>
      <a href="{{ path('app_trajet_map', {'idTrajet': trajet.idTrajet}) }}" class="btn btn-info rounded-pill m-2" ><i class="fas fa-location-arrow"></i></a>
               
       <br>
       <div class="text-center">
        <a href="#">
          <i class="fas fa
 
       <br>
       <div class="text-center">
        <a href="{{path('app_reservation_newc')}}">
          <i  class="fas fa-book" style="margin-right: 0.5em;"></i>Reserve
        </div>
      </a>
    </div>
  </div>
{% else %}
  <p>no records found</p>
{% endfor %}


	{% endblock %}
